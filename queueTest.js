exports.createUser = function (name) {

};

exports.friendsWith = function (users) {

};

exports.users = function () {
  var self = this;

  self.users = 
};


exports.users = function () {
  var queue = new Queue(); 

  users.forEach(function(el) {

  queue.add(function(resp) {
    // userGotCreated
    

   });
 });
};

// Look into how superagent does this with .end();
// I'll do it with .users();

fetchTweet(url)
  .linkify()
  .filterBadWords()
  .appendTo('#status');

fbTest
  .createUser("Magic Man")
  // friends with knows about "magic man" and a list of users
  // so if you call users() before friendsWith, it'll return `Magic Man`
  .friendsWith(["El Diablo"]) 
  .users();



